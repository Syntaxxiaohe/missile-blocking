# Worklog

更新时间：2026-03-14

## 1. 当前整体状态

目前论文主体已经完成从“模型草稿”到“可投稿版本”的主要收口工作。中文主稿、英文稿框架、图表、实验结果、附录统计、作者信息和核心图示均已整理到 `paper_draft` 目录下，中文稿可以正常编译，结果部分已经用最终实验数据回填。

当前最接近定稿的主文件为：

- `paper_draft/main_paper.tex`
- `paper_draft/main_paper.pdf`
- `paper_draft/main_paper_mathematics_en.tex`

## 2. 已完成工作

### 2.1 论文结构与表述统一

- 统一了摘要、引言、方法、讨论、结论对 `M1 / M2 / M3` 的定义。
- 将 `M3` 明确修正为“虚拟席位扩充下的离散 OT 全局分配”，不再混入旧版“弹药惩罚”叙述。
- 按审稿意见系统清理了口语化、解释性过强、版本说明式表述。
- 将中文稿的方法节改写为更偏数学论文的叙述方式：建模假设、变量定义、公式推导、命题、实现说明分层更清楚。

### 2.2 公式与理论部分修订

- 完整重写了三阶段导引状态机，使其数学表达闭合。
- 重写扩散模型推导链条，区分：
  - 严格扩散关系
  - 一阶线性化
  - 线性胀宽建模近似
  - 误差界
- 补充了 `3σ` 空间剪枝误差上界说明。
- 修正了 Beer-Lambert 主推导与实现剪枝之间的逻辑顺序。
- 明确说明了六条代表性视线是 `representative-ray approximation / observability surrogate`，而非严格几何等价。
- 在 `M2` 中指出并处理了列偏置项不改变 Hungarian 最优排列的问题。
- 在 `M3` 中指出并处理了时间项仅为列偏置、不改变 OT 指派结构的问题。
- 补入了 `M3` 的结构性命题：全覆盖与容量上界保证。

### 2.3 实验与结果回填

- 已从 `all result` 目录读取并整理最终实验结果。
- 结果节已补入：
  - 总体成功率
  - 综合效费比
  - 四场景分场景成功率
  - Wilson 区间
  - Welch t 检验
  - Mann-Whitney U 检验
- 已补充附录统计表，覆盖分场景均值、样本标准差、区间和两两比较。
- 已补充代理近似验证，包括：
  - 六视线近似一致性检查
  - ETA surrogate 与高保真时刻的偏差量级检查

### 2.4 图表与可视化完善

已新增或重绘的关键图包括：

- 成功率热力图
- 完整小提琴 + 箱线图 + 原始散点分布图
- 烟团浓度热力图重绘版本
- 烟团质心平流与尺度胀宽示意图
- 六条代表性视线示意图
- `3σ` 剪枝局部几何图
- `S4` 下 `M2 / M3` 结构对比图

其中，`S4` 结构对比图已按最新实验链路重做，不再使用旧的固定波次文件，而是：

- 从 `终极实验M3.zip` 中的最新代码读取逻辑
- 从 `mode_4_full_360_swarm` 导弹池抽样
- 固定 `eval_idx = 0`
- 真实运行 `M2` 与 `M3` 的任务组织器后绘制

同时，论文中已经补充说明：

- `M3` 不是最近邻分配
- 单次评估下 `N_m = 20, N_u = 5`
- 因而 `K = ceil(20 / 5) = 4`
- 每架 UAV 最多承接 `4` 枚导弹
- 某些看起来“更近”的导弹仍可能被分配给其他 UAV，这是容量约束下的全局最优分配结果

### 2.5 文档与交付整理

- `paper_draft` 目录已整理为可打包外发的论文源码目录。
- 中文稿当前可直接在本地编译通过。
- 作者信息已补入：
  - 谭卓
  - 吴迪
  - 作者待定
  - 汪伟平
- 共同一作与通讯作者标注已加入中英文稿。

## 3. 当前仍需关注的问题

以下问题不再属于“模型逻辑错误”，但仍影响最终投稿质量：

- 英文稿尚未完全同步中文稿的最新全部修改，尤其是新增图、附录统计、结构图说明等。
- 图题、图注、中英文术语仍可再做一轮统一。
- 参考文献和个别长公式段落仍有少量排版 warning。
- 第二作者姓名仍为占位状态，需要最终确认。
- 少数图仍可进一步美化版式，尤其是局部说明框的密度与 caption 长度。

## 4. 建议的后续工作方向

### 4.1 优先级最高

1. 完成英文稿同步
2. 完成最终作者信息确认
3. 全文再做一轮终稿化语言统一
4. 重新检查图题、表题、引用、附录编号

### 4.2 建议补充的内容

1. 敏感性分析

建议优先做局部敏感性分析，而不是直接上高成本全局方法。推荐参数包括：

- 光学强度因子
- 扩散速度
- 风速幅值
- 失锁阈值或视场阈值
- `M2` 的空间阈值 `D_c`
- `M2` 的时间阈值 `T_c`

控制原则建议如下：

- 一次只改变一个参数，其余保持不变
- 三种方法使用同一组随机种子和同一批导弹样本
- 参数变化后重新优化 `M1 / M2 / M3`
- 主文报告总体成功率与关键场景结果
- 附录补充结构指标和运行时间

2. 计算代价比较

建议补一个简短小表，统一汇总：

- 单次评估时间
- 优化迭代预算
- 种群规模
- 各方法编码维数

3. 复现实验设置说明

建议再单列一个短段，写清：

- 随机抽样机制
- 每类场景重复次数
- 硬件环境
- 随机种子控制方式

### 4.3 如时间允许可继续强化

1. 增加六视线近似与更高密度视线采样的对比实验
2. 增加 surrogate ETA 与高保真 ETA 对分组结果影响的一致性实验
3. 对风场和烟幕参数做更广范围的鲁棒性分析
4. 将结构图、结果图、物理引擎图全部同步到英文稿

## 5. 推荐的近期执行顺序

建议按以下顺序继续推进：

1. 同步英文稿
2. 确认第二作者实名
3. 完成一次全文终稿排版检查
4. 补灵敏度分析
5. 统一生成投稿压缩包

## 6. 备注

- `paper_draft` 已可作为当前对外共享的论文源码目录。
- 如需发给合作者，建议直接整体打包 `paper_draft`。
- 如需继续扩展实验，建议以当前中文稿为主版本推进，英文稿后同步。
